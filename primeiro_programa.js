// Cria as variaveis para a mensagem de boas vindas
var greeting = 'Howdy ';
var nome = 'Molly ';
var = ', please check your order:';
// Conecta as tres variaveis acima para criar a mensagem de boas vindas
var welcome = greeting + name + message;

// Cria variaveis para armazenar detalhes sobre a placa
var sing = 'Montague House';
var tiles = sing.length;
var subTotal = tiles * 5;
var shipping = 7;
var grandTotal = subTotal + shipping


var el = document.getElementById('greenting');


el.textContent = welcome;


var elSing = document.getElementById('userSing');
elSing.textContent = sing;
